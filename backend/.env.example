# SQLite (default): relative to cwd when starting uvicorn from backend/
# DATABASE_URL=sqlite:///./data/ft.db

# PostgreSQL example:
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/ft

# Root directory for dataset files and future model artifacts
# STORAGE_ROOT=./storage
